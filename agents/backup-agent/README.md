# backup-agent installers (placeholder)

No install scripts exist yet — today the registration flow (`docs/backup.agent/registration.md` in `NextDeveloper/S3`) only documents a bare CLI command the customer runs manually. When a real installer is built for Linux/Windows/macOS, it belongs here, following the same pattern as `agents/vm-service`.
